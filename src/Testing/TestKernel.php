<?php

declare(strict_types=1);

namespace Arcanum\Testing;

/**
 * Test harness kernel for app developers.
 *
 * Composes a real HyperKernel and RuneKernel internally so tests dispatch
 * through the same lifecycle production code does. This skeleton is a
 * placeholder — behavior lands in subsequent commits.
 */
final class TestKernel
{
    public function __construct()
    {
    }
}
