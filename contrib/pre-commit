#!/bin/bash

# Only run full checks if PHP files are staged.
STAGED_PHP=$(git diff --cached --name-only --diff-filter=ACMR | grep '\.php$')

if [ -z "$STAGED_PHP" ]; then
    exit 0
fi

composer check
